# Cloudflare Pages configuration for static site
# This file prevents Wrangler errors when deploying to Cloudflare Pages
# For static sites, Cloudflare Pages handles deployment automatically

name = "opensecflow-website"
compatibility_date = "2024-01-01"

# Static site configuration
pages_build_output_dir = "dist"
